import type { JSX } from 'react';

/**
 * Placeholder shell. The UI programmer replaces this component with the real
 * wallet interface, building on top of the modules in `src/lib/`.
 */
export default function App(): JSX.Element {
  return (
    <main>
      <h1>Simple Bitcoin Wallet</h1>
      <p>UI coming soon.</p>
    </main>
  );
}
